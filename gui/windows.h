/* Zircon windows header — references kernel GUI as the single source of truth */
#ifndef ZIRCON_WINDOWS_H
#define ZIRCON_WINDOWS_H

/* The canonical window declarations live in kernel/gui/window.h.
 * This header exists for Zircon toolkit compatibility only. */
#include "../../kernel/gui/window.h"

#endif
